<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
            "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

<!--    properties标签：1、可以定义键值对 2、可以引用属性文件-->
        <properties resource="jdbc.properties"/>

<!--    设置mybatis的属性(缓存，延迟加载)-->
    <!--    <settings>-->
    <!--        <setting name="" value=""/>-->
    <!--    </settings>-->

<!--    typeAliases标签用于给实体类取别名，在映射文件中可以直接使用类名代替实体类的全限定名-->
    <!--    <typeAliases>-->
    <!--        <typeAlias type="" alias=""/>-->
    <!--    </typeAliases>-->

<!--plugins标签，⽤于配置MyBatis插件（分⻚插件）-->
        <plugins>
            <plugin interceptor="com.github.pagehelper.PageInterceptor"/>
        </plugins>

<!-- 在environments配置数据库连接信息 -->
    <!-- 在environments标签中可以定义多个environment标签，每个environment标签可以定义⼀套连接配置 -->
    <!-- default属性，⽤来指定使⽤哪个environment标签 -->
        <environments default="mysql">
            <environment id="mysql">
        <!--transactionManager标签⽤于配置数据库管理⽅式-->
                <!--type="JDBC" 可以进⾏事务的提交和回滚操作-->
                <!--type="MANAGED" 依赖容器完成事务管理，本身不进⾏事务的提交和回滚操作 -->
                <transactionManager type="JDBC"/>
            <!--dataSource标签就是⽤来配置数据库连接信息 -->
                <dataSource type="POOLED">
                    <property name="driver" value="${mysql_driver}"/>
                    <property name="url" value="${mysql_url}"/>
                    <property name="username" value="${mysql_username}"/>
                    <property name="password" value="${mysql_password}"/>
                </dataSource>
            </environment>

        </environments>

<!--mappers标签⽤于载⼊映射⽂件-->
        <mappers >
            <mapper resource="mapper/BookMapper.xml"/>
            <mapper resource="mapper/MultiTableMapper.xml"/>
            <mapper class="com.hexiang.mybatis.dao.AnnotationBookDao"/>
        </mappers>

</configuration>
